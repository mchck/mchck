Examples for the MC HCK
=======================

Preparation
-----------

Install the system build files:

	sh ../toolchain/extract.sh $path_to_NUC100SeriesBSP system

Now you have the headers in system/, where the examples expect them.
